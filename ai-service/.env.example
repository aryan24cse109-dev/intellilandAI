# OpenRouter is OpenAI-compatible. Leave this unset to enable the clearly
# labelled ground-truth fallback for the bundled synthetic sample documents.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openrouter/free
AI_SERVICE_PORT=8000
